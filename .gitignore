# OS
.DS_Store

# Node / build artifacts
node_modules/
web/.output/
web/.nitro/
web/.tanstack/
dist/
*.log

# Source data — 4.8 GB of Climate TRACE CSVs (one file is 2.3 GB, far over
# GitHub's 100 MB/file limit). Not committed; see README "Data" for how to obtain.
DEU/DATA/

# Local env / secrets
.env
.env.*
!.env.example

# Personal Claude Code settings (keep shared launch.json, ignore local grants)
.claude/settings.local.json

# Spark ivy cache (if ever run on host)
.ivy/

# Gradle (Kotlin Spark job build)
.gradle/
spark/kotlin/build/
